# Greenfield

Placeholder for a new app that is **fully independent** from Aria.

Rename this folder when you know the product name (e.g. `apps/my-product/`).

## Setup

```bash
cd apps/greenfield
npm install
npm run dev
```

## Isolation

- Do not import from `../../src` or `../../api`.
- Do not change files at the repository root for this app.
- Add dependencies only in this folder's `package.json`.

Replace this README and scaffold files when you bootstrap the real stack (Vite, Next.js, etc.).
